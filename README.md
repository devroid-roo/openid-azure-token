# openid-azure-token
A lightweight Laravel package for Azure Entra ID (OpenID Connect) authentication using token-only sign-in. This package is built on top of Laravel Socialite and SocialiteProviders. It validates the Azure id_token, reads user identity from JWT claims.
